services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: rpgxuser
      POSTGRES_PASSWORD: hxhxpassword
      POSTGRES_DB: rpgxdb
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: